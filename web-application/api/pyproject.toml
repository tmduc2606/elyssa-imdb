[tool.pytest.ini_options]
cache_dir = ".pytest_cache"
testpaths = ["tests"]

[tool.ruff]
target-version = "py313"
line-length = 120

[tool.ruff.lint.per-file-ignores]
# Deliberate sys.path bootstrap before imports (container path shims)
"app/api/router.py" = ["E402"]
"app/models/gmu.py" = ["E402"]
